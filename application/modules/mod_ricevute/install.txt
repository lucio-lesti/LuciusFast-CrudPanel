DROP TABLE IF EXISTS mod_ricevute;
CREATE TABLE IF NOT EXISTS mod_ricevute (
 `id` int(10) NOT NULL auto_increment,
 `tess_asc` varchar(255) NULL,
 `tessera` varchar(255) NULL,
 `fk_anagrafica` int(10) NULL,
 `nome` varchar(255) NULL,
 `cognome` varchar(255) NULL,
 `fk_disciplina` int(10) NULL,
 `disciplina` varchar(255) NULL,
 `importo` varchar(255) NULL,
 `data` varchar(255) NULL,
 `mese` enum(9) NULL,
 `anno` enum(4) NULL,
PRIMARY KEY (`id`)
) ENGINE=MyISAM  DEFAULT CHARSET=utf8 AUTO_INCREMENT=1;	
	
DELETE FROM core_module_list WHERE mod_name = 'mod_ricevute';
	
DELETE FROM core_roles_permission WHERE mod_name = 'mod_ricevute';
	
INSERT INTO core_module_list(mod_id,mod_name, class_name, mod_type, mod_title, mod_icon,mod_parentid, active, is_deletable,is_moveable,position)
		VALUES(178,'mod_ricevute','mod_ricevute','mod_gen','Pagamenti/Ricevute','fa fa-euro',17,'Y','Y','Y',56);
INSERT INTO core_roles_permission(role_id,mod_id,mod_name,perm_read,perm_write,perm_update,perm_delete)
			VALUES(1,178,'mod_ricevute','Y','Y','Y','Y')